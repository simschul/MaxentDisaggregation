% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ragg.R
\name{tnorm_ab_params_from_moments}
\alias{tnorm_ab_params_from_moments}
\title{Truncated Normal parameters from target mean m and sd s}
\usage{
tnorm_ab_params_from_moments(m, s, a, b, tol = 1e-10, maxit = 100)
}
\arguments{
\item{m}{numeric scalar, target mean of truncated variable X (must be finite)}

\item{s}{numeric scalar, target sd of truncated variable X (s > 0, finite)}

\item{a}{numeric scalar, lower bound (can be -Inf)}

\item{b}{numeric scalar, upper bound (can be  Inf); must have a < b}

\item{tol}{absolute tolerance for the residual norm in the 2D solve}

\item{maxit}{maximum iterations for the 2D Newton solver}
}
\value{
list(mu=..., sigma=..., alpha=..., beta=..., method="closed|shift|upper|2d-newton", iters=...)
}
\description{
Given target moments (m, s) for X = Y | a <= Y <= b, Y ~ Normal(mu, sigma^2),
return (mu, sigma) such that the truncated distribution matches (m, s).
Handles all cases: untruncated, one-sided, and two-sided truncation.
}
