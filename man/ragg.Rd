% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ragg.R
\name{ragg}
\alias{ragg}
\title{Aggregate Random Number Generator}
\usage{
ragg(
  n,
  mean = NULL,
  sd = NULL,
  min = -Inf,
  max = Inf,
  meanlog = NULL,
  sdlog = NULL,
  log = FALSE
)
}
\arguments{
\item{n}{Integer. Number of random values to generate.}

\item{mean}{Numeric. Mean of the distribution on the \emph{linear} scale (for Normal,
Truncated Normal, Lognormal parameterization, or Exponential).
Set to \code{NULL} if not used.}

\item{sd}{Numeric. Standard deviation of the distribution on the \emph{linear} scale
(for Normal, Truncated Normal, or Lognormal parameterization).
Set to \code{NULL} if not used.}

\item{min}{Numeric. Lower bound (for Truncated Normal or Uniform). Defaults to \code{0}.}

\item{max}{Numeric. Upper bound (for Truncated Normal or Uniform). Defaults to \code{Inf}.}

\item{meanlog}{Numeric. Mean on the log scale for a standard lognormal draw (when
\code{mean}/\code{sd} are \code{NULL}). Defaults to \code{NULL}.}

\item{sdlog}{Numeric. Standard deviation on the log scale for a standard lognormal
draw (when \code{mean}/\code{sd} are \code{NULL}). Defaults to \code{NULL}.}

\item{log}{Logical. If \code{TRUE}, interpret the distribution as lognormal
(given \code{mean} and \code{sd} on the linear scale). Defaults to \code{FALSE}.}
}
\value{
A numeric vector of length \code{n}, drawn from the specified distribution.
The distribution choice is determined by the argument combination.
}
\description{
Generates random numbers from a variety of distributions, chosen by the
combination of provided parameters:
}
\details{
\itemize{
\item \strong{Normal} distribution (if \code{mean} and \code{sd} are given, \code{min = -Inf}, \code{max = Inf}, and \code{log = FALSE})
\item \strong{Lognormal} distribution (if \code{mean} and \code{sd} are given, \code{min <= 0}, \code{max = Inf}, and \code{log = TRUE})
\item \strong{Truncated Normal} distribution (if \code{mean} and \code{sd} are given, and finite \code{min}, \code{max} not both infinite)
\item \strong{Exponential} distribution (if \code{mean} is given, \code{sd} is \code{NULL}, and \code{min = 0}, \code{max = Inf})
\item \strong{Uniform} distribution (if both \code{mean} and \code{sd} are \code{NULL}, and finite \code{min} and \code{max})
\item \strong{Lognormal} via meanlog/sdlog** (if \code{meanlog} and \code{sdlog} are not \code{NULL} and \code{mean}/\code{sd} are \code{NULL})
}

If none of these distribution signatures is met, an error is raised.
}
\examples{
# 1) Normal distribution
x1 <- ragg(10, mean = 5, sd = 1)

# 2) Lognormal distribution (via linear-scale mean and sd)
x2 <- ragg(10, mean = 1, sd = 0.3, log = TRUE)

# 3) Truncated Normal distribution
x3 <- ragg(10, mean = 5, sd = 1, min = 3, max = 7)

# 4) Exponential distribution
x4 <- ragg(10, mean = 2)

# 5) Uniform distribution
x5 <- ragg(10, min = 0, max = 10)

# 6) Standard lognormal (using meanlog, sdlog)
x6 <- ragg(10, meanlog = 0, sdlog = 1)
}
\seealso{
\code{\link[=rnorm]{rnorm()}}, \code{\link[=rlnorm]{rlnorm()}}, \code{\link[=rlnorm2]{rlnorm2()}}, \code{\link[=rtruncnorm]{rtruncnorm()}}, \code{\link[=rexp]{rexp()}}, \code{\link[=runif]{runif()}}
}
